from typing import Optional


def google_search(search_terms: str, days_lookback: Optional[int] = None):
    """Search google for the given search terms.

    Parameters
    ----------
    search_terms : str
        The terms to search for.
    days_lookback : Optional[int], optional
        Number of days to lookback, by default None. 0 means today only.
    """
    pass
